name: Deploy

on:
  push:
    branches: [main, dev]
  workflow_dispatch:

jobs:
  deploy:
    uses: AtlasReaper311/atlas-infra/.github/workflows/deploy-worker.yml@main
    with:
      worker_name: ramone-trigger
      working_directory: worker
      run_lint: true
      run_tests: false
    secrets: inherit
