name: Deploy

on:
  push:
    branches: [main, dev]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.repository }}-deploy-${{ github.ref }}
  cancel-in-progress: false

jobs:
  deploy:
    uses: AtlasReaper311/atlas-infra/.github/workflows/deploy-worker.yml@d05564499b1f6822430f9427a451207cc06ebe4f # atlas-infra main
    with:
      worker_name: ramone-trigger
      working_directory: worker
      run_lint: true
      run_tests: false
    secrets: inherit
